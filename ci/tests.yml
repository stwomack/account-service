---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: bankmonitor/spring-boot
    tag: 'latest-jar'

inputs:
- name: account-service

run:
  path: bash
  args: ['-c', 'mvn install']