---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: bankmonitor/spring-boot
    tag: 'latest-jar'

inputs:
- name: simple-go-webapp

run:
  path: bash
  args: ['-c', 'mvn install']