platform: linux

image_resource:
  type: docker-image
  source:
    repository: sonarqube
    tag: latest

#inputs:
#  - name: account-service

run:
  path: "mvnw"
  args: ["-f", "account-service/pom.xml", "package", "sonar:sonar"]

params:
  MAVEN_BASEDIR: account-service