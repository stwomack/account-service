---
applications:
  - name: account-service	
    host: account-${random-word}
    path: target/account-service-0.0.1-SNAPSHOT.jar
    memory: 512MB
    instances: 1
    buildpack: java_buildpack_offline
    services:
      - capps-logger
      - my-postgres
#      - my-mysql
    env:
      SPRING_PROFILES_ACTIVE: dev
