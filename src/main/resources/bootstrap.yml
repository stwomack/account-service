#spring.
server:
  port: 8080
spring:
  application:
    name: account-service
    profiles:
      active: dev,mysql
  jpa:
    hibernate:
      ddl-auto: create
  cloud:
    config:
      uri: http://localhost:8000